#!/bin/bash

# repo forall -j 100 -c 'pwd; git status -s'


repo forall -j 100 -c '
out="$(git -c color.status=always status -s)"
if [[ -n "$out" ]] ; then
    pwd
    echo "$out"
fi
'

# exec repo-parallel '
# out="$(git -c color.status=always status -s)"
# if [[ -n "$out" ]] ; then
#     pwd
#     echo "$out"
# fi
# '

#!/bin/bash

if [[ -n "$SSH_AGENT_PID" ]] ; then
	exit 0
fi

# $HOME/tmp/ is on ramdisk
saved=$HOME/tmp/ssh-agent-vars

if [[ -f $saved ]] ; then
	cat $saved
	exit 0
fi

ssh-agent -s | tee $saved
exit 0
